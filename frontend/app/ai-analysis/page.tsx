import { UnavailableFeature } from "@/components/ui/UnavailableFeature";
export default function AiAnalysisPage() { return <UnavailableFeature title="AI analysis endpoint unavailable" detail="AI results are stored with each log and incident by the current backend. A separate AI analysis or statistics endpoint is not available, so LogIQ will not fabricate insights here."/> }
