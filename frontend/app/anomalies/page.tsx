import { UnavailableFeature } from "@/components/ui/UnavailableFeature";
export default function AnomaliesPage() { return <UnavailableFeature title="Anomaly workspace awaiting API" detail="The current backend detects anomalies while logs are created, but does not expose an anomalies list, statistics, or detail endpoint. Connect one of those endpoints to activate this workspace."/> }
