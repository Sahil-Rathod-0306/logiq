"use client";
import { useAuth } from "@/context/AuthContext";
import { UserRound } from "lucide-react";
export default function ProfilePage() { const { user }=useAuth(); return <div className="max-w-2xl"><div className="rounded-xl border border-slate-800 bg-[#0d1423] p-6"><div className="flex items-center gap-4"><span className="grid size-14 place-items-center rounded-full bg-cyan-400/10 text-lg font-bold text-cyan-300">{user?.name.slice(0,2).toUpperCase()||"--"}</span><div><h2 className="text-xl font-semibold text-white">{user?.name||"Profile"}</h2><p className="text-sm text-slate-400">Account information from the active session</p></div></div><dl className="mt-8 divide-y divide-slate-800">{[["Name",user?.name],["Email",user?.email],["Role",user?.role]].map(([label,value])=><div key={label as string} className="flex justify-between gap-4 py-4 text-sm"><dt className="text-slate-500">{label}</dt><dd className="text-right text-slate-200">{value||"Unavailable"}</dd></div>)}</dl><p className="mt-5 text-xs text-slate-500">Profile editing is unavailable because the backend exposes no profile update endpoint.</p></div></div> }
