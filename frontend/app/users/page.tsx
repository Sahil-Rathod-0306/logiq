"use client";
import { useEffect, useState } from "react";
import { getUsers } from "@/services/user.service";
import type { User } from "@/types";
import { UsersRound } from "lucide-react";
export default function UsersPage() { const [users,setUsers]=useState<User[]|null>(null),[forbidden,setForbidden]=useState(false); useEffect(()=>{getUsers().then(setUsers).catch(error=>setForbidden(error.response?.status===403))},[]); return <div className="max-w-6xl space-y-6"><div><h2 className="text-xl font-semibold text-white">Users</h2><p className="mt-1 text-sm text-slate-400">Admin-controlled user directory.</p></div><section className="overflow-hidden rounded-xl border border-slate-800 bg-[#0d1423]">{forbidden?<div className="grid min-h-64 place-items-center p-8 text-center"><div><UsersRound className="mx-auto mb-3 text-slate-500"/><h3 className="font-medium text-white">Admin access required</h3><p className="mt-1 text-sm text-slate-400">Your backend only permits administrators to view users.</p></div></div>:!users?<div className="p-8 text-sm text-slate-500">Loading user directory…</div>:<div className="overflow-x-auto"><table className="w-full min-w-150 text-left text-sm"><thead className="border-b border-slate-800 bg-slate-900/50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Name</th><th className="px-5 py-3">Email</th><th className="px-5 py-3">Role</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{users.map(user=><tr key={user.id} className="border-b border-slate-800/80 last:border-0"><td className="px-5 py-4 font-medium text-slate-200">{user.name}</td><td className="px-5 py-4 text-slate-400">{user.email}</td><td className="px-5 py-4"><span className="rounded bg-cyan-400/10 px-2 py-1 text-xs font-semibold text-cyan-300">{user.role}</span></td><td className="px-5 py-4 text-emerald-300">Active</td></tr>)}</tbody></table></div>}</section><p className="text-xs text-slate-500">Role changes are not shown because this backend supports them only through an admin PATCH action and this interface currently has no user-confirmation workflow.</p></div> }
