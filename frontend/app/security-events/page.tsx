import { UnavailableFeature } from "@/components/ui/UnavailableFeature";
export default function SecurityEventsPage() { return <UnavailableFeature title="Security events endpoint unavailable" detail="The backend records security events internally but currently has no route for listing or viewing them. Add an existing read endpoint to enable this page."/> }
